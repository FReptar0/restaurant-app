import React from 'react'

export const metadata = {
    title: 'Restaurante - Sign Up',
    description: 'Root layout for the app',
};


export default function SignupLayour(props) {
    return (
        <>
            {props.children}
        </>
    )
}
